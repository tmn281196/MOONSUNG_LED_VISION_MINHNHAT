<Window x:Class="LEDVision.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LEDVision"
        mc:Ignorable="d"
        Title="About"
        Width="520" Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        FontFamily="Segoe UI"
        Background="White">

    <!--  Popup giới thiệu: 2 logo (Moonsung, TNG) + tên / phiên bản app  -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="Images/Moonsung Icon.png" Stretch="Uniform" MaxHeight="150" Margin="8" />
            <Rectangle Grid.Column="1" Width="1" Fill="#DDE3EA" Margin="8,16" />
            <Image Grid.Column="2" Source="Images/TNG Logo.png" Stretch="Uniform" MaxHeight="150" Margin="8" />
        </Grid>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,12,0,0">
            <TextBlock Text="LED COLOR INSPECTION" FontSize="20" FontWeight="Bold" Foreground="#323F4E" HorizontalAlignment="Center" />
            <TextBlock x:Name="versionText" Text="Version 2.0.0.0" FontSize="12" Foreground="#6B7A8C" HorizontalAlignment="Center" Margin="0,2,0,0" />
            <TextBlock Text="Moonsung  ·  TNG R&amp;D" FontSize="12" Foreground="#6B7A8C" HorizontalAlignment="Center" Margin="0,2,0,0" />
        </StackPanel>

        <Button Grid.Row="2" Content="Close" Width="90" Height="28" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click" />
    </Grid>
</Window>
